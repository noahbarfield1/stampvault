# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...but the template a fresh clone needs MUST stay tracked.
!.env.local.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local scratch, agent tooling, generated reports, abandoned firebase build output.
# Evidence worth keeping from e2e-reports/ is archived under docs/verification/.
/scratch/
/graphify-out/
/.agents/
/.firebase/
/e2e-reports/
*.bundle
*.patch
firebase-debug.log
